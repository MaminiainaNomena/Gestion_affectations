<%@ taglib prefix="c" uri="jakarta.tags.core" %><%@ include file="header.jspf" %><div class="card"><h1>${empty affecter ? 'Nouvelle affectation' : 'Modifier affectation'}</h1><form method="post" action="${pageContext.request.contextPath}/affectations"><input type="hidden" name="oldCodeemp" value="${affecter.id.codeemp}"><input type="hidden" name="oldCodelieu" value="${affecter.id.codelieu}"><div class="form-grid"><label class="field">Employé<select name="codeemp" required><c:forEach var="e" items="${employes}"><option value="${e.codeemp}" ${affecter.employe.codeemp == e.codeemp ? 'selected' : ''}>${e.codeemp} — ${e.nom} ${e.prenom}</option></c:forEach></select></label><label class="field">Lieu<select name="codelieu" required><c:forEach var="l" items="${lieux}"><option value="${l.codelieu}" ${affecter.lieu.codelieu == l.codelieu ? 'selected' : ''}>${l.codelieu} — ${l.designation}</option></c:forEach></select></label><label class="field">Date<input type="date" name="date" required value="${affecter.date}"></label></div><br><button class="btn">Enregistrer</button> <a class="btn secondary" href="${pageContext.request.contextPath}/affectations">Annuler</a></form></div><%@ include file="footer.jspf" %>
